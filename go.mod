// Deprecated: github.com/duynhlab/pkg no longer publishes Go packages.
// The single-module line is frozen at v0.35.0; import the per-module
// paths instead (github.com/duynhlab/pkg/<module>, tagged
// <module>/vX.Y.Z). This placeholder exists so a final root tag can
// vacate the old package paths and keep mixed old+new dependency
// graphs resolvable.
module github.com/duynhlab/pkg

go 1.25.8
