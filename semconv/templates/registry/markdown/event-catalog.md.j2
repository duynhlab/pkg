<!-- generated by weaver from duynhlab/pkg semconv/registry — do not edit; run make semconv-generate -->
| Event | Emitted when | Attributes |
|---|---|---|
{%- for event in ctx.events | sort(attribute="name") %}
| `{{ event.name }}` | {{ event.brief | replace("Emitted when: ", "") | trim(".") }} | {% for a in event.attributes %}`{{ a.name }}`{{ ", " if not loop.last }}{% endfor %} |
{%- endfor %}
