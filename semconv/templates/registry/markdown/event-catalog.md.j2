<!-- generated by weaver from duynhlab/pkg semconv/registry — do not edit; run make semconv-generate -->
| Class | Event | Emitted at | Attributes | Owner |
|---|---|---|---|---|
{%- for event in ctx.events | sort(attribute="name") %}
| {{ event.annotations.class }} | `{{ event.name }}` | {{ event.brief | replace("Emitted when: ", "") | trim(".") }} | {% for a in event.attributes %}`{{ a.name }}`{{ ", " if not loop.last }}{% endfor %} | {{ event.annotations.owner }} |
{%- endfor %}

