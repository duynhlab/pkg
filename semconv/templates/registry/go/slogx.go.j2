// Code generated by weaver from semconv/ (the platform registry). DO NOT EDIT.
// Regenerate with `make semconv-generate`.

package slogx

// Attribute keys the facade writes, as the registry names them.
const (
{%- for attr in ctx.attributes %}
	// {{ attr.brief }}
	Key{{ attr.name | pascal_case }} = "{{ attr.name }}"
{%- endfor %}
)

// Lifecycle event names the facade owns.
const (
{%- for event in ctx.events %}
	// {{ event.brief }}
	Event{{ event.name | pascal_case }} = "{{ event.name }}"
{%- endfor %}
)

