<!-- generated by weaver from duynhlab/pkg semconv/registry — do not edit; run make semconv-generate -->
| Event | Emitted when | Attributes |
|---|---|---|
| `checkout.session.confirmed` | session handed to order | `checkout.session.id`, `order.id` |
| `checkout.session.expired` | session expired | `checkout.session.id`, `reason` |
| `checkout.session.requoted` | confirm sent the buyer back to requote | `checkout.session.id`, `reason` |
| `inventory.reservation.rejected` | reserve refused | `inventory.reservation.ref`, `outcome` |
| `order.cancelled` | cancellation workflow reaches `cancelled` | `order.epoch`, `order.id` |
| `order.compensation.completed` | one compensation step finishes | `compensation.step`, `error.type`, `order.id`, `outcome` |
| `order.confirmed` | saga moves the order to `confirmed` | `order.id` |
| `order.created` | order row committed as `pending` | `order.id` |
| `order.failed` | saga ends the order `failed` | `order.id`, `outcome`, `reason` |
| `order.manual_review.entered` | order parked for a human decision | `order.id`, `reason` |
| `order.retry.exhausted` | a bounded retry gives up and the order is escalated | `attempts`, `error.type`, `operation`, `order.id` |
| `payment.authorization.completed` | authorize decision stored | `order.id`, `outcome`, `payment.id` |
| `payment.capture.completed` | capture decision stored | `outcome`, `payment.id` |
| `payment.reconciliation.discrepancy.detected` | reconciliation run finds a mismatch | `discrepancy.class`, `reconciliation.run_id` |
| `payment.refund.completed` | refund settled | `outcome`, `payment.id`, `refund.id` |
| `process.started` | entry point ready to serve | `component` |
| `process.stopped` | entry point finished shutting down | `component`, `outcome` |
| `temporal.workflow.failed` | a run ends failed, terminated or timed out | `temporal.run_status`, `temporal.workflow.type` |
| `temporal.workflow.started` | the client starts a workflow | `temporal.task_queue`, `temporal.workflow.type` |